{
  "topic": "K-Nearest Neighbors (KNN)",
  "definition": "A type of instance-based learning algorithm that is used for both classification and regression, but mainly used for classification tasks in machine learning.",
  "key_points": [
    "It works by finding the K closest training samples in the feature space to the test sample and then assigning the label that is most common amongst these K samples.",
    "The choice of K can have a significant impact on the algorithm's performance, with larger values of K leading to smoother decision boundaries but potentially increased sensitivity to noise.",
    "KNN does not make any assumptions about the underlying distribution of data, making it non-parametric and flexible."
  ],
  "example": "For example, if we have a dataset of iris flowers where each flower is described by several features (like petal length, petal width, sepal length, and sepal width), KNN can be used to classify new flowers based on their features. In this case, the number of nearest neighbors (K) might be set to 3."
}